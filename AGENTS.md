# Project Overview: Hybrid Wardrobe & Styling App

## 1. 프로젝트 목적 및 핵심 구조

본 프로젝트는 React Native(Expo)를 활용한 옷장 및 코디네이션 앱이다.
사용자 개인의 경험은 '오프라인 퍼스트(Offline-first)'로 지연 없이 작동해야 하며, 향후 타 계정과의 공유를 위해 클라우드 스토리지(Supabase)와 로컬 캐싱을 혼합한 하이브리드 아키텍처를 따른다.

- **Frontend:** React Native (Expo)
- **Local DB:** Expo SQLite (또는 MMKV)
- **Cloud/Backend:** Supabase (Auth, Database, Storage)
- **핵심 데이터 흐름:** 이미지는 Supabase Storage에 업로드 후 URL을 받아 로컬 DB에 저장하고, 동시에 기기 로컬 캐시에 다운로드하여 오프라인 뷰를 지원한다. 백업 시에는 이미지 파일 자체가 아닌 URL이 포함된 JSON 데이터를 추출한다.

## 2. 작업 원칙: 페이즈별 순차 진행

프로젝트의 복잡도를 낮추기 위해 **반드시 지정된 페이즈(Phase) 순서대로 개발을 진행**한다. 하나의 페이즈가 완전히 테스트되고 오류가 없을 때만 다음 페이즈로 넘어간다.

- **Phase 1: 로컬 기반 뼈대 구축**
  - UI 레이아웃, 카메라/갤러리 접근, 로컬 DB(SQLite) 연동.
  - 옷 데이터(브랜드, 색상, 계절 등 메타데이터) 입력 및 조회 화면 구현.
- **Phase 2: 이미지 가공 및 캔버스 구현**
  - 이미지 압축 및 온디바이스 배경 제거(투명화) 로직 적용.
  - 투명화된 옷 이미지를 드래그 앤 드롭, 크기 조절, 회전이 가능한 코디 스티커북(Canvas) 기능 구현.
- **Phase 3: 하이브리드 캐싱 및 클라우드 연동**
  - Supabase 연동.
  - 옷 등록 시 Supabase Storage에 이미지 업로드 -> URL 반환 -> 기기 로컬 캐시에 이미지 저장 -> 로컬 DB에 메타데이터와 URL 동시 저장.
- **Phase 4: 데이터 동기화 및 백업/공유**
  - 로컬 DB의 텍스트 데이터(JSON) Export/Import 백업 기능.
  - 친구 추가 및 타 계정 옷장 조회(온라인) 기능.

## 3. 에이전트(Codex) 행동 및 코드 규칙

1. **로그 기록 의무 (Data Tracking):**

   - 모델 변환 및 파인튜닝 추적을 위해, 코드를 수정하거나 주요 결정을 내릴 때마다 프로젝트 최상단의 `CHANGELOG.md`파일에 내역을 기록한다.
   - 기록 포맷: `[Date] | [Task/Phase] | [Action Taken] | [Reason/Purpose]`
   - 이 기록은 LLM이 직접 엑셀을 다룰 때 발생하는 파일 손상 문제를 방지하기 위해 텍스트 기반 형식(CSV)으로 관리한다.

2. **단순함과 모듈화:**

   - 코드는 과도하게 추상화하지 않고 직관적으로 작성한다.
   - UI 컴포넌트와 비즈니스 로직(DB 접근, 상태 관리)을 철저히 분리한다.

3. **문제 해결 방식:**

   - 에러가 발생하면 임의로 라이브러리를 추가하거나 구조를 뒤엎지 말고, 발생한 에러 로그와 함께 원인을 먼저 분석하여 나(사용자)에게 보고한 후 수정한다.
   - 오프라인 상태와 온라인 상태의 분기 처리를 명확히 하여 네트워크가 끊겨도 개인 옷장 조회 앱이 크래시되지 않도록 예외 처리를 강제한다.

4. **디자인:**
   - 프로젝트 최상단의 `design.md`를 참고할 것
